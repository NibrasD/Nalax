# Quick Wallet — محفظة Stellar محلية بسيطة

دمج Privy تم إزالته. أصبح الدخول الآن بزر واحد بدون أي خدمات خارجية.

## كيف تعمل

```
المستخدم يضغط "محفظة فورية"
        ↓
toggle: [إنشاء جديدة] أو [استيراد موجودة]
        ↓
[إنشاء جديدة]
  - Keypair عشوائي يُولَّد محلياً (stellar-sdk)
  - يُحفظ في localStorage
  - Friendbot يُموّل الحساب على testnet
  - تظهر للمستخدم: العنوان (G...) + المفتاح السري (S...) للنسخ الاحتياطي
        ↓
[استيراد موجودة]
  - يُلصق المفتاح السري (S...)
  - يُحفظ في localStorage
        ↓
كل معاملات Soroban تُوقَّع محلياً بـ tx.sign(keypair)
```

## مزايا هذا الحل

- ✅ **بسيط** — زر واحد، بدون email أو OTP أو popups
- ✅ **سريع** — جاهز خلال ثانيتين
- ✅ **بدون اعتمادات خارجية** — لا API keys، لا Privy/Magic/Web3Auth
- ✅ **خاص** — المفتاح لا يُرسَل لأي خادم
- ✅ **استمرارية** — المحفظة محفوظة في localStorage بين الجلسات
- ✅ **استيراد/تصدير** — المستخدم يُسيطر بالكامل عبر المفتاح السري

## التحذيرات

- ⚠️ **Testnet فقط** — الأمان الأمثل لـ MVP/تجربة، لا للإنتاج بأموال حقيقية
- ⚠️ **localStorage** — مسح بيانات المتصفح يحذف المحفظة (لذا التنزيل/النسخ الاحتياطي مهم)
- ⚠️ **عبر الأجهزة** — كل جهاز محفظة منفصلة، يحتاج المستخدم استيراد المفتاح يدوياً

## للإنتاج (Mainnet)

عند النقل لـ mainnet، استبدل localStorage بأحد:
1. **Privy TEE** — بعد تفعيل Stellar (Tier 2) في Privy account
2. **Hardware wallet** — Ledger/Trezor عبر Stellar SDK
3. **Backend مع تشفير** — السيرفر يحفظ مفاتيح المستخدمين مشفّرة

## الملفات المهمة

| الملف | الوظيفة |
|-------|---------|
| `src/lib/quick-wallet.ts` | كل منطق المحفظة (إنشاء، استيراد، تحميل، توقيع) |
| `src/store/useWallet.ts` | يدعم `quick-wallet` و `freighter` كمزودَين |
| `src/components/EmailAuthModal.tsx` | UI الإنشاء/الاستيراد (تبويبان) |
| `src/lib/stellar.ts` | `registerPrivySigner` يستقبل دالة `signXdrLocally` |
| `src/main.tsx` | `QuickWalletBridge` يستعيد المحفظة من localStorage عند التحميل |

## الإعداد

```bash
npm install
npm run dev
```

لا حاجة لأي متغيرات بيئة للمحفظة. اضغط "محفظة فورية" وستعمل فوراً.
